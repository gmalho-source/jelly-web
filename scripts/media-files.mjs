/**
 * O mapa de tipos vive em `src/lib/mime.ts`, que é o que o site também usa.
 * Este ficheiro só o reexporta para os guiões que já o importavam daqui.
 */
export { mimeFor, extensaoDe } from "../src/lib/mime.ts";
